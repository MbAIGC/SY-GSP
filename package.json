{
  "name": "sy-gsp",
  "version": "0.1.0",
  "private": true,
  "description": "SY-GSP —— 思源笔记 GitHub/Gitee 同步插件(同步引擎 2.0 全新源码实现)",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "build": "node build.mjs",
    "smoke": "node smoke/verify.mjs",
    "verify": "npm run test && npm run build && npm run smoke"
  },
  "dependencies": {
    "node-diff3": "^3.1.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
